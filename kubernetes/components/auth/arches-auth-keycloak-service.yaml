apiVersion: v1
kind: Service
metadata:
  labels:
    component: arches-auth-keycloak
  name: arches-auth-keycloak
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
  selector:
    component: arches-auth-keycloak
status:
  loadBalancer: {}
