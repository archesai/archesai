apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - postgres-deployment.yaml
  - postgres-service.yaml

secretGenerator:
  - name: archesai-config-env
    behavior: merge
    literals:
      - ARCHES_DATABASE_URL=postgresql://admin:admin@postgres:5432/nestjs?schema=public
