apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    component: milvus-standalone-data
  name: milvus-standalone-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: standard
