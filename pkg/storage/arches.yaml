# yaml-language-server: $schema=../../pkg/config/api/components/schemas/Config.yaml
generation:
  spec: ./api/openapi.yaml
  includes:
    - server
  only:
    - models
    - handlers
    - routes
    - app_handlers
    - app_infrastructure
    - app_routes
