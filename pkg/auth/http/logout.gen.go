// Code generated by archesai. DO NOT EDIT.

package http

import (
	"errors"
	"log/slog"
	"net/http"

	"github.com/google/uuid"

	"github.com/archesai/archesai/pkg/auth/operations"
	"github.com/archesai/archesai/pkg/httputil"
	"github.com/archesai/archesai/pkg/server"
)

// ============================================================================
// Logout - POST /auth/logout
// ============================================================================

// LogoutHandler is the HTTP handler for Logout.
type LogoutHandler struct {
	logout operations.Logout
}

// NewLogoutHandler creates a new HTTP handler.
func NewLogoutHandler(logout operations.Logout) *LogoutHandler {
	return &LogoutHandler{logout: logout}
}

// RegisterLogoutRoute registers the HTTP route for Logout.
func RegisterLogoutRoute(mux *http.ServeMux, handler *LogoutHandler) {
	mux.HandleFunc("POST /auth/logout", handler.ServeHTTP)
}

// Response Types

// Logout200Response is the 200 success response.
type Logout200Response struct {
	Message string `json:"message"`
}

// StatusCode returns the HTTP status code.
func (Logout200Response) StatusCode() int { return 200 }

// Logout400Response is the 400 error response.
type Logout400Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (Logout400Response) StatusCode() int { return 400 }

// Logout401Response is the 401 error response.
type Logout401Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (Logout401Response) StatusCode() int { return 401 }

// Logout422Response is the 422 error response.
type Logout422Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (Logout422Response) StatusCode() int { return 422 }

// Logout429Response is the 429 error response.
type Logout429Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (Logout429Response) StatusCode() int { return 429 }

// Logout500Response is the 500 error response.
type Logout500Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (Logout500Response) StatusCode() int { return 500 }

// ServeHTTP handles the POST /auth/logout endpoint.
func (h *LogoutHandler) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()

	// Extract session ID from context for authenticated operations
	sessionID, ok := ctx.Value(server.SessionIDContextKey).(uuid.UUID)
	if !ok {
		_ = httputil.WriteResponse(w, Logout401Response{
			ProblemDetails: httputil.NewUnauthorizedResponse("session required", r.URL.Path),
		})
		return
	}

	// Build input from request
	input := &operations.LogoutInput{}
	input.SessionID = sessionID

	// Execute handler
	result, err := h.logout.Execute(ctx, input)
	if err != nil {
		var httpErr httputil.HTTPError
		if errors.As(err, &httpErr) {
			_ = httputil.WriteHTTPError(w, httpErr, r.URL.Path)
			return
		}
		slog.Error("handler error", "operation", "Logout", "error", err)
		_ = httputil.WriteResponse(w, Logout500Response{
			ProblemDetails: httputil.NewInternalServerErrorResponse(err.Error(), r.URL.Path),
		})
		return
	}

	// Build and send response
	_ = httputil.WriteResponse(w, Logout200Response{
		Message: result.Message,
	})
}
