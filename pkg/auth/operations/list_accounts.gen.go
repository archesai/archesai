// Code generated by archesai. DO NOT EDIT.

package operations

import (
	"context"
	"fmt"

	"github.com/archesai/archesai/pkg/auth/schemas"
	"github.com/archesai/archesai/pkg/executor"
	serverschemas "github.com/archesai/archesai/pkg/server/schemas"
	"github.com/google/uuid"
)

// ============================================================================
// ListAccounts
// ============================================================================

// ListAccountsInput represents the input for the ListAccounts operation.
type ListAccountsInput struct {
	SessionID      uuid.UUID
	AccountsFilter *serverschemas.FilterNode
	AccountsSort   *serverschemas.FilterNode
	Page           serverschemas.Page
}

// ListAccountsOutput represents the output for the ListAccounts operation.
type ListAccountsOutput struct {
	Data []schemas.Account            `json:"data"`
	Meta serverschemas.PaginationMeta `json:"meta"`
}

// ListAccounts defines the executor interface for the ListAccounts operation.
type ListAccounts = executor.Executor[ListAccountsInput, ListAccountsOutput]

// ListAccountsImpl is the default implementation of ListAccounts.
type ListAccountsImpl struct {
	repo schemas.AccountRepository
}

// NewListAccounts creates a new ListAccounts handler.
func NewListAccounts(
	repo schemas.AccountRepository,
) ListAccounts {
	return &ListAccountsImpl{
		repo: repo,
	}
}

// Execute performs the ListAccounts operation.
func (h *ListAccountsImpl) Execute(ctx context.Context, input *ListAccountsInput) (*ListAccountsOutput, error) {
	// Pagination parameters with defaults
	limit := int32(100)
	offset := int32(0)
	if input.Page.Limit != nil {
		limit = *input.Page.Limit
	}
	if input.Page.Offset != nil {
		offset = *input.Page.Offset
	}

	// List from repository
	results, total, err := h.repo.List(ctx, limit, offset)
	if err != nil {
		return nil, fmt.Errorf("failed to list accounts: %w", err)
	}

	// Convert pointer slice to value slice
	data := make([]schemas.Account, len(results))
	for i, r := range results {
		data[i] = *r
	}

	output := &ListAccountsOutput{
		Data: data,
		Meta: serverschemas.PaginationMeta{
			Total: int32(total),
		},
	}

	return output, nil
}
