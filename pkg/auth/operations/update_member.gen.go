// Code generated by archesai. DO NOT EDIT.

package operations

import (
	"context"
	"fmt"
	"time"

	"github.com/archesai/archesai/pkg/auth/schemas"
	"github.com/archesai/archesai/pkg/events"
	"github.com/archesai/archesai/pkg/executor"
	"github.com/google/uuid"
)

// ============================================================================
// UpdateMember
// ============================================================================

// UpdateMemberInput represents the input for the UpdateMember operation.
type UpdateMemberInput struct {
	SessionID      uuid.UUID
	OrganizationID uuid.UUID
	ID             uuid.UUID
	Role           *string
}

// UpdateMemberOutput represents the output for the UpdateMember operation.
type UpdateMemberOutput struct {
	Data schemas.Member `json:"data"`
}

// UpdateMember defines the executor interface for the UpdateMember operation.
type UpdateMember = executor.Executor[UpdateMemberInput, UpdateMemberOutput]

// UpdateMemberImpl is the default implementation of UpdateMember.
type UpdateMemberImpl struct {
	repo      schemas.MemberRepository
	publisher events.Publisher
}

// NewUpdateMember creates a new UpdateMember handler.
func NewUpdateMember(
	repo schemas.MemberRepository,
	publisher events.Publisher,
) UpdateMember {
	return &UpdateMemberImpl{
		repo:      repo,
		publisher: publisher,
	}
}

// Execute performs the UpdateMember operation.
func (h *UpdateMemberImpl) Execute(ctx context.Context, input *UpdateMemberInput) (*UpdateMemberOutput, error) {
	// Get existing entity
	existing, err := h.repo.Get(ctx, input.ID)
	if err != nil {
		return nil, fmt.Errorf("failed to get member: %w", err)
	}

	// Update fields
	// TODO: Map input fields to entity
	existing.UpdatedAt = time.Now().UTC()

	// Save to repository
	updated, err := h.repo.Update(ctx, input.ID, existing)
	if err != nil {
		return nil, fmt.Errorf("failed to update member: %w", err)
	}

	// Publish event
	event := schemas.NewMemberUpdatedEvent(updated.ID)
	_ = h.publisher.Publish(ctx, event)

	// Map to output
	output := &UpdateMemberOutput{
		// TODO: Map updated entity to output
	}
	_ = updated

	return output, nil
}
