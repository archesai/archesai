// Code generated by archesai. DO NOT EDIT.

package operations

import (
	"context"
	"fmt"

	"github.com/archesai/archesai/pkg/auth/schemas"
	"github.com/archesai/archesai/pkg/executor"
	serverschemas "github.com/archesai/archesai/pkg/server/schemas"
	"github.com/google/uuid"
)

// ============================================================================
// ListUsers
// ============================================================================

// ListUsersInput represents the input for the ListUsers operation.
type ListUsersInput struct {
	SessionID   uuid.UUID
	UsersFilter *serverschemas.FilterNode
	UsersSort   *serverschemas.FilterNode
	Page        serverschemas.Page
}

// ListUsersOutput represents the output for the ListUsers operation.
type ListUsersOutput struct {
	Data []schemas.User               `json:"data"`
	Meta serverschemas.PaginationMeta `json:"meta"`
}

// ListUsers defines the executor interface for the ListUsers operation.
type ListUsers = executor.Executor[ListUsersInput, ListUsersOutput]

// ListUsersImpl is the default implementation of ListUsers.
type ListUsersImpl struct {
	repo schemas.UserRepository
}

// NewListUsers creates a new ListUsers handler.
func NewListUsers(
	repo schemas.UserRepository,
) ListUsers {
	return &ListUsersImpl{
		repo: repo,
	}
}

// Execute performs the ListUsers operation.
func (h *ListUsersImpl) Execute(ctx context.Context, input *ListUsersInput) (*ListUsersOutput, error) {
	// Pagination parameters with defaults
	limit := int32(100)
	offset := int32(0)
	if input.Page.Limit != nil {
		limit = *input.Page.Limit
	}
	if input.Page.Offset != nil {
		offset = *input.Page.Offset
	}

	// List from repository
	results, total, err := h.repo.List(ctx, limit, offset)
	if err != nil {
		return nil, fmt.Errorf("failed to list users: %w", err)
	}

	// Convert pointer slice to value slice
	data := make([]schemas.User, len(results))
	for i, r := range results {
		data[i] = *r
	}

	output := &ListUsersOutput{
		Data: data,
		Meta: serverschemas.PaginationMeta{
			Total: int32(total),
		},
	}

	return output, nil
}
