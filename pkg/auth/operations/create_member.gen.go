// Code generated by archesai. DO NOT EDIT.

package operations

import (
	"context"
	"fmt"
	"time"

	"github.com/archesai/archesai/pkg/auth/schemas"
	"github.com/archesai/archesai/pkg/events"
	"github.com/archesai/archesai/pkg/executor"
	"github.com/google/uuid"
)

// ============================================================================
// CreateMember
// ============================================================================

// CreateMemberInput represents the input for the CreateMember operation.
type CreateMemberInput struct {
	SessionID      uuid.UUID
	OrganizationID uuid.UUID
	Role           string
}

// CreateMemberOutput represents the output for the CreateMember operation.
type CreateMemberOutput struct {
	Data schemas.Member `json:"data"`
}

// CreateMember defines the executor interface for the CreateMember operation.
type CreateMember = executor.Executor[CreateMemberInput, CreateMemberOutput]

// CreateMemberImpl is the default implementation of CreateMember.
type CreateMemberImpl struct {
	repo      schemas.MemberRepository
	publisher events.Publisher
}

// NewCreateMember creates a new CreateMember handler.
func NewCreateMember(
	repo schemas.MemberRepository,
	publisher events.Publisher,
) CreateMember {
	return &CreateMemberImpl{
		repo:      repo,
		publisher: publisher,
	}
}

// Execute performs the CreateMember operation.
func (h *CreateMemberImpl) Execute(ctx context.Context, input *CreateMemberInput) (*CreateMemberOutput, error) {
	// Create entity
	entity := &schemas.Member{
		ID:        uuid.New(),
		CreatedAt: time.Now().UTC(),
		UpdatedAt: time.Now().UTC(),
		// TODO: Map input fields to entity
	}

	// Save to repository
	created, err := h.repo.Create(ctx, entity)
	if err != nil {
		return nil, fmt.Errorf("failed to create member: %w", err)
	}

	// Publish event
	event := schemas.NewMemberCreatedEvent(created.ID)
	_ = h.publisher.Publish(ctx, event)

	// Map to output
	output := &CreateMemberOutput{
		// TODO: Map created entity to output
	}
	_ = created

	return output, nil
}
