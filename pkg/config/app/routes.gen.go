// Code generated by archesai. DO NOT EDIT.

package app

import (
	"log/slog"
	"net/http"

	"github.com/archesai/archesai/pkg/config/routes"
)

// Routes holds all HTTP route handlers for this package.
type Routes struct {
	GetConfig *routes.GetConfigHandler
}

// NewRoutes creates all route handlers from the given handlers.
func NewRoutes(h *Handlers) *Routes {
	return &Routes{
		GetConfig: routes.NewGetConfigHandler(h.GetConfig),
	}
}

// RegisterRoutes registers all routes for this package with the http.ServeMux.
func RegisterRoutes(mux *http.ServeMux, r *Routes) {
	slog.Info("registering route", "method", "GET", "path", "/config")
	routes.RegisterGetConfigRoute(mux, r.GetConfig)
}
