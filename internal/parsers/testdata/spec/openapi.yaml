openapi: 3.1.0
info:
  title: Test Server API
  version: 1.0.0
tags:
  - name: Server
    description: Server operations
paths:
  /health:
    get:
      operationId: getHealth
      tags:
        - Server
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
