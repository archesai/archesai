// Code generated by codegen events. DO NOT EDIT.
package events

import (
	"context"
	"encoding/json"
	"fmt"
	"time"

	"github.com/archesai/archesai/internal/auth"
	"github.com/google/uuid"
	"github.com/redis/go-redis/v9"
)

// RedisEventPublisher implements EventPublisher using Redis pub/sub.
type RedisEventPublisher struct {
	client  *redis.Client
	channel string
}

// NewRedisEventPublisher creates a new Redis event publisher.
func NewRedisEventPublisher(client *redis.Client) auth.EventPublisher {
	channel := "auth-events"

	return &RedisEventPublisher{
		client:  client,
		channel: channel,
	}
}

// NewRedisEventPublisherWithChannel creates a new Redis event publisher with custom channel.
func NewRedisEventPublisherWithChannel(client *redis.Client, channel string) auth.EventPublisher {
	return &RedisEventPublisher{
		client:  client,
		channel: channel,
	}
}

// PublishAccountCreated publishes a created event event for Account.
func (p *RedisEventPublisher) PublishAccountCreated(ctx context.Context, entity *auth.Account) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventAccountCreated,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Account",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishAccountUpdated publishes a updated event event for Account.
func (p *RedisEventPublisher) PublishAccountUpdated(ctx context.Context, entity *auth.Account) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventAccountUpdated,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Account",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishAccountDeleted publishes a deleted event event for Account.
func (p *RedisEventPublisher) PublishAccountDeleted(ctx context.Context, entity *auth.Account) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventAccountDeleted,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Account",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishAccountLinked publishes a linked event event for Account.
func (p *RedisEventPublisher) PublishAccountLinked(ctx context.Context, entity *auth.Account) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventAccountLinked,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Account",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishAccountUnlinked publishes a unlinked event event for Account.
func (p *RedisEventPublisher) PublishAccountUnlinked(ctx context.Context, entity *auth.Account) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventAccountUnlinked,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Account",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishSessionCreated publishes a created event event for Session.
func (p *RedisEventPublisher) PublishSessionCreated(ctx context.Context, entity *auth.Session) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventSessionCreated,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Session",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishSessionRefreshed publishes a refreshed event event for Session.
func (p *RedisEventPublisher) PublishSessionRefreshed(ctx context.Context, entity *auth.Session) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventSessionRefreshed,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Session",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishSessionExpired publishes a expired event event for Session.
func (p *RedisEventPublisher) PublishSessionExpired(ctx context.Context, entity *auth.Session) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventSessionExpired,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Session",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishSessionDeleted publishes a deleted event event for Session.
func (p *RedisEventPublisher) PublishSessionDeleted(ctx context.Context, entity *auth.Session) error {
	if entity == nil {
		return fmt.Errorf("entity cannot be nil")
	}

	event := &auth.Event{
		ID:        uuid.New().String(),
		Type:      auth.EventSessionDeleted,
		Timestamp: time.Now().UTC(),
		Source:    "auth",
		Data:      entity,
		Metadata: map[string]string{
			"entity_id":   entity.Id.String(),
			"entity_type": "Session",
		},
	}

	return p.PublishRaw(ctx, event)
}

// PublishRaw publishes a raw event to Redis.
func (p *RedisEventPublisher) PublishRaw(ctx context.Context, event *auth.Event) error {
	if event == nil {
		return fmt.Errorf("event cannot be nil")
	}

	// Ensure event has required fields
	if event.ID == "" {
		event.ID = uuid.New().String()
	}
	if event.Timestamp.IsZero() {
		event.Timestamp = time.Now().UTC()
	}
	if event.Source == "" {
		event.Source = "auth"
	}

	// Marshal event to JSON
	data, err := json.Marshal(event)
	if err != nil {
		return fmt.Errorf("failed to marshal event: %w", err)
	}

	// Publish to Redis channel
	if err := p.client.Publish(ctx, p.channel, data).Err(); err != nil {
		return fmt.Errorf("failed to publish event to Redis: %w", err)
	}

	// Also publish to type-specific channel for selective subscription
	typeChannel := fmt.Sprintf("%s:%s", p.channel, string(event.Type))
	if err := p.client.Publish(ctx, typeChannel, data).Err(); err != nil {
		// Log error but don't fail - type-specific channel is optional
		// In production, you'd use proper logging here
		_ = err
	}

	return nil
}

// Subscribe subscribes to domain events.
func (p *RedisEventPublisher) Subscribe(ctx context.Context, handler func(event *auth.Event) error) error {
	// Create subscription
	pubsub := p.client.Subscribe(ctx, p.channel)
	defer pubsub.Close()

	// Wait for subscription confirmation
	_, err := pubsub.Receive(ctx)
	if err != nil {
		return fmt.Errorf("failed to subscribe to channel: %w", err)
	}

	// Get channel for messages
	ch := pubsub.Channel()

	// Process messages
	for {
		select {
		case <-ctx.Done():
			return ctx.Err()
		case msg := <-ch:
			if msg == nil {
				continue
			}

			// Unmarshal event
			var event auth.Event
			if err := json.Unmarshal([]byte(msg.Payload), &event); err != nil {
				// Log error but continue processing
				// In production, you'd use proper logging here
				continue
			}

			// Handle event
			if err := handler(&event); err != nil {
				// Log error but continue processing
				// In production, you'd use proper logging here
				continue
			}
		}
	}
}

// SubscribeToType subscribes to specific event types.
func (p *RedisEventPublisher) SubscribeToType(ctx context.Context, eventType auth.EventType, handler func(event *auth.Event) error) error {
	// Subscribe to type-specific channel
	typeChannel := fmt.Sprintf("%s:%s", p.channel, string(eventType))

	// Create subscription
	pubsub := p.client.Subscribe(ctx, typeChannel)
	defer pubsub.Close()

	// Wait for subscription confirmation
	_, err := pubsub.Receive(ctx)
	if err != nil {
		return fmt.Errorf("failed to subscribe to type channel: %w", err)
	}

	// Get channel for messages
	ch := pubsub.Channel()

	// Process messages
	for {
		select {
		case <-ctx.Done():
			return ctx.Err()
		case msg := <-ch:
			if msg == nil {
				continue
			}

			// Unmarshal event
			var event auth.Event
			if err := json.Unmarshal([]byte(msg.Payload), &event); err != nil {
				// Log error but continue processing
				continue
			}

			// Handle event
			if err := handler(&event); err != nil {
				// Log error but continue processing
				continue
			}
		}
	}
}
