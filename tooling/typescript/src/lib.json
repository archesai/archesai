{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "tsBuildInfoFile": "${configDir}/.cache/tsconfig.lib.tsbuildinfo"
  },
  "exclude": [
    "${configDir}/src/**/*.spec.ts",
    "${configDir}/src/**/*.spec.tsx"
  ],
  "include": ["${configDir}/src"]
}
