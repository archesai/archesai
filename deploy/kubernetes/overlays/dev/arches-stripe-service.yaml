apiVersion: v1
kind: Service
metadata:
  name: arches-stripe
  labels:
    component: arches-stripe
spec:
  selector:
    component: arches-stripe
  ports:
    - name: arches-stripe-port
      port: 3000
      targetPort: 3000
