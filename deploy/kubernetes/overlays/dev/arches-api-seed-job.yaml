apiVersion: batch/v1
kind: Job
metadata:
  name: arches-api-seed
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: arches-api-seed
          image: arches-api:dev
          envFrom:
            - secretRef:
                name: arches-api-config-secret
