apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    component: minio-data
  name: minio-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: standard
