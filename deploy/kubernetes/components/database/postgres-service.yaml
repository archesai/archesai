apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30054
      protocol: TCP
      name: postgres-node-port
  selector:
    component: postgres
  type: NodePort
