apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  ports:
    - port: 6379
      nodePort: 30055
      targetPort: 6379
      protocol: TCP
      name: redis-node-port
  selector:
    component: redis
  type: NodePort
