apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: logging
spec:
  type: LoadBalancer # Change to ClusterIP if using Ingress
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: grafana
