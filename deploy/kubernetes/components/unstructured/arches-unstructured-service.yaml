apiVersion: v1
kind: Service
metadata:
  labels:
    component: arches-unstructured
  name: arches-unstructured
spec:
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
  selector:
    component: arches-unstructured
