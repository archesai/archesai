apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: archesai
spec:
  ingressClassName: nginx
  rules:
    - host: api.localhost
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: api
                port:
                  number: 443
