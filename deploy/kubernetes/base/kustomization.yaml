apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: archesai

resources:
  - api
  - migrations
  - namespace.yaml

secretGenerator:
  - name: archesai-config-base
    files:
      - config.yaml
  - name: archesai-config-volume
