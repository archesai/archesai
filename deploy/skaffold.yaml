apiVersion: skaffold/v4beta5
kind: Config
profiles:
  - name: staging
    deploy:
      statusCheckDeadlineSeconds: 300
      tolerateFailuresUntilDeadline: true
    manifests:
      kustomize:
        paths:
          - "kubernetes/overlays/gcp/stage"
  - name: production
    deploy:
      statusCheckDeadlineSeconds: 300
      tolerateFailuresUntilDeadline: true
    manifests:
      kustomize:
        paths:
          - "kubernetes/overlays/gcp/production"
