apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "arches.fullname" . }}-config
  namespace: {{ .Values.infrastructure.namespace }}
  labels:
    {{- include "arches.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "arches.config" . | nindent 4 }}