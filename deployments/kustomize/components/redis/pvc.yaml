apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archesai-redis
  labels:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
