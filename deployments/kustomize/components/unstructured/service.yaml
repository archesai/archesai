apiVersion: v1
kind: Service
metadata:
  name: archesai-unstructured
  labels:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: unstructured
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: unstructured
