apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: archesai-platform
  labels:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - platform.archesai.local
      secretName: archesai-platform-tls
  rules:
    - host: platform.archesai.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: archesai-platform
                port:
                  number: 3000
