apiVersion: v1
kind: Service
metadata:
  name: archesai-storage
  labels:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: storage
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: api
      protocol: TCP
      name: api
    - port: 9001
      targetPort: console
      protocol: TCP
      name: console
  selector:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: storage
