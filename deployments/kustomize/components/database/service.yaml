apiVersion: v1
kind: Service
metadata:
  name: archesai-postgres
  labels:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: postgres
