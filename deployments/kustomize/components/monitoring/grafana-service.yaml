apiVersion: v1
kind: Service
metadata:
  name: archesai-grafana
  labels:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: grafana
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: grafana
