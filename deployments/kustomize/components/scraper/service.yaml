apiVersion: v1
kind: Service
metadata:
  name: archesai-scraper
  labels:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: scraper
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: archesai
    app.kubernetes.io/component: scraper
