// Code generated by archesai. DO NOT EDIT.

package http

import (
	"errors"
	"log/slog"
	"net/http"

	"github.com/archesai/archesai/pkg/httputil"
	serverschemas "github.com/archesai/archesai/pkg/server/schemas"
	"github.com/archesai/examples/basic/operations"
	"github.com/archesai/examples/basic/schemas"
)

// ============================================================================
// ListTodos - GET /todos
// ============================================================================

// ListTodosHandler is the HTTP handler for ListTodos.
type ListTodosHandler struct {
	listTodos operations.ListTodos
}

// NewListTodosHandler creates a new HTTP handler.
func NewListTodosHandler(listTodos operations.ListTodos) *ListTodosHandler {
	return &ListTodosHandler{listTodos: listTodos}
}

// RegisterListTodosRoute registers the HTTP route for ListTodos.
func RegisterListTodosRoute(mux *http.ServeMux, handler *ListTodosHandler) {
	mux.HandleFunc("GET /todos", handler.ServeHTTP)
}

// Response Types

// ListTodos200Response is the 200 success response.
type ListTodos200Response struct {
	Data []schemas.Todo               `json:"data"`
	Meta serverschemas.PaginationMeta `json:"meta"`
}

// StatusCode returns the HTTP status code.
func (ListTodos200Response) StatusCode() int { return 200 }

// ListTodos400Response is the 400 error response.
type ListTodos400Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (ListTodos400Response) StatusCode() int { return 400 }

// ListTodos401Response is the 401 error response.
type ListTodos401Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (ListTodos401Response) StatusCode() int { return 401 }

// ListTodos422Response is the 422 error response.
type ListTodos422Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (ListTodos422Response) StatusCode() int { return 422 }

// ListTodos429Response is the 429 error response.
type ListTodos429Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (ListTodos429Response) StatusCode() int { return 429 }

// ListTodos500Response is the 500 error response.
type ListTodos500Response struct {
	httputil.ProblemDetails
}

// StatusCode returns the HTTP status code.
func (ListTodos500Response) StatusCode() int { return 500 }

// ServeHTTP handles the GET /todos endpoint.
func (h *ListTodosHandler) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()

	// Build input from request
	input := &operations.ListTodosInput{}

	// Query parameter "todosFilter"
	if err := httputil.BindQueryParam(r, "todosFilter", &input.TodosFilter); err != nil {
		_ = httputil.WriteResponse(w, ListTodos400Response{
			ProblemDetails: httputil.NewBadRequestResponse(err.Error(), r.URL.Path),
		})
		return
	}

	// Query parameter "todosSort"
	if err := httputil.BindQueryParam(r, "todosSort", &input.TodosSort); err != nil {
		_ = httputil.WriteResponse(w, ListTodos400Response{
			ProblemDetails: httputil.NewBadRequestResponse(err.Error(), r.URL.Path),
		})
		return
	}

	// Query parameter "page"
	if err := httputil.BindQueryParam(r, "page", &input.Page); err != nil {
		_ = httputil.WriteResponse(w, ListTodos400Response{
			ProblemDetails: httputil.NewBadRequestResponse(err.Error(), r.URL.Path),
		})
		return
	}

	// Execute handler
	result, err := h.listTodos.Execute(ctx, input)
	if err != nil {
		var httpErr httputil.HTTPError
		if errors.As(err, &httpErr) {
			_ = httputil.WriteHTTPError(w, httpErr, r.URL.Path)
			return
		}
		slog.Error("handler error", "operation", "ListTodos", "error", err)
		_ = httputil.WriteResponse(w, ListTodos500Response{
			ProblemDetails: httputil.NewInternalServerErrorResponse(err.Error(), r.URL.Path),
		})
		return
	}

	// Build and send response
	_ = httputil.WriteResponse(w, ListTodos200Response{
		Data: result.Data,
		Meta: result.Meta,
	})
}
