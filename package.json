{
  "name": "@archesai/archesai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "clean": "git clean -xdf node_modules .cache .turbo .next dist",
    "clean:workspaces": "turbo run clean",
    "dev": "turbo watch dev --continue -F !@archesai/landing",
    "dev:api": "turbo watch dev -F @archesai/api",
    "dev:landing": "turbo watch dev -F @archesai/landing",
    "dev:platform": "turbo watch dev -F @archesai/platform",
    "format": "turbo run format --continue -- --cache --cache-location .cache/.prettiercache",
    "format:fix": "turbo run format --continue -- --write --cache --cache-location .cache/.prettiercache",
    "preinstall": "only-allow pnpm",
    "postinstall": "sherif",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache",
    "lint:fix": "turbo run lint --continue -- --fix --cache --cache-location .cache/.eslintcache",
    "prepare": "husky",
    "typecheck": "turbo run typecheck"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx,json,css,md,yaml}": [
      "pnpm prettier --write --ignore-path .gitignore --log-level warn"
    ]
  },
  "prettier": "@archesai/prettier/base",
  "devDependencies": {
    "@archesai/eslint": "workspace:*",
    "@archesai/prettier": "workspace:*",
    "@archesai/typescript": "workspace:*",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "only-allow": "^1.2.1",
    "prettier": "catalog:",
    "sherif": "^1.6.1",
    "turbo": "^2.5.4",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=24.2.0",
    "pnpm": "^10.12.4"
  }
}
