components:
  schemas:
    AccountEntity:
      description: Schema for Account entity
      type: object
      properties:
        createdAt:
          description: The date this item was created
          type: string
          format: date-time
        id:
          description: The ID of the item
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        updatedAt:
          description: The date this item was last updated
          type: string
          format: date-time
        accessToken:
          description: The access token
          type: string
          nullable: true
        accessTokenExpiresAt:
          description: The expiration date
          type: string
          nullable: true
        accountId:
          description: The unique identifier for the account
          type: string
        idToken:
          description: The ID token
          type: string
          nullable: true
        password:
          description: The hashed password for local authentication
          type: string
          nullable: true
        providerId:
          description: The provider ID associated with the auth provider
          type: string
        refreshToken:
          description: The refresh token
          type: string
          nullable: true
        refreshTokenExpiresAt:
          description: The refresh token expiration date
          type: string
          nullable: true
        scope:
          description: The scope of the access token
          type: string
          nullable: true
        userId:
          description: The user ID associated with the auth provider
          type: string
      required:
        - createdAt
        - id
        - updatedAt
        - accessToken
        - accessTokenExpiresAt
        - accountId
        - idToken
        - password
        - providerId
        - refreshToken
        - refreshTokenExpiresAt
        - scope
        - userId
      additionalProperties: false
    # These schemas are now simplified - the filtering logic is handled in paths.yaml
    # by composing the common FilterNode with field restrictions
