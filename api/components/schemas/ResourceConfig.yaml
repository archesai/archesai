# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
description: Kubernetes resource configuration
type: object
properties:
  limits:
    $ref: ./ResourceLimits.yaml
  requests:
    $ref: ./ResourceRequests.yaml
required:
  - limits
  - requests
additionalProperties: false
