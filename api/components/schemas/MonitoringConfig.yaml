description: Monitoring configuration for Grafana and Loki
type: object
properties:
  grafana:
    $ref: ./GrafanaConfig.yaml
  loki:
    $ref: ./LokiConfig.yaml
required:
  - grafana
  - loki
additionalProperties: false
