description: Pipeline step response
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          $ref: ../schemas/PipelineStep.yaml
      required:
        - data
      additionalProperties: false
headers:
  X-RateLimit-Limit:
    $ref: ../headers/RateLimitLimit.yaml
  X-RateLimit-Remaining:
    $ref: ../headers/RateLimitRemaining.yaml
  X-RateLimit-Reset:
    $ref: ../headers/RateLimitReset.yaml
